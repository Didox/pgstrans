#json.extract! dispositivo, :id, :nome, :marca, :modelo, :numero_serie, :macaddr, :imei, :imei2, :rated, :screensize, :so, :ram_rom, :conectividade, :created_at, :updated_at
#json.url dispositivo_url(dispositivo, format: :json)
