<div class="col-md-12 scroltable">
	<div class="col-md-12">
    <p id="notice"><%= notice %></p>
    <h3>Parâmetros de Integração</h3>
  </div>
  <!-- Page Heading/Breadcrumbs -->
  <%= render :partial => 'shared/header_backoffice' %>
  <!-- /.row -->
  <div class="col-md-12 scroltable">
    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">Parceiro</th>
          <th scope="col">URL Integração em Desenvolvimento</th>
          <th scope="col">URL Integração em Produção</th>
          <th colspan="3">Ações para o Registo</th>
        </tr>
      </thead>

      <tbody>
        <% @parametros.each do |parametro| %>
        <tr>
          <td><%= parametro.partner.name %> <%= parametro.get.categoria.to_s.upcase %></td>
          <td><%= parametro.get.url_integracao_desenvolvimento %></td>
          <td><%= parametro.get.url_integracao_producao %></td>
          <td><%= link_to 'Mostrar', parametro %></td>
          <td><%= link_to 'Editar', edit_parametro_path(parametro) %></td>
          <td><%= link_to 'Apagar', parametro, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
        </tr>
        <% end %>
      </tbody>
    </table>

    <div class="apple_pagination">
      <%= will_paginate @parametros, :container => false %>
    </div>    

    <%= link_to new_parametro_path, class: 'btn btn-success', style: "width: 150px;text-align: center;margin-right: 0px;" do %>
      <span class="glyphicon glyphicon-plus"></span>
      Novo Registo
    <% end %>    

    <%= link_to backoffice_index_path, class: 'btn btn-primary', style: "width: 150px;text-align: center;margin-right: 0px;" do %>
      <span class="glyphicon glyphicon-arrow-left"></span>
      Voltar
    <% end %> 

  </div>
</div>
