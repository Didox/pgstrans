json.extract! venda, :id, :product_id, :agent_id, :store_id, :seller_id, :terminal_id, :value, :customer_number, :request_send, :response_get, :created_at, :updated_at
json.url venda_url(venda, format: :json)
