<div class="col-md-12">
  <div class="col-md-12">
    <h3>Detalhes da Venda</h3>
  </div>

  <!-- Page Heading/Breadcrumbs -->
  <%= render :partial => 'shared/header_backoffice' %>
  <!-- /.row -->  

  <div class="col-md-12">
    <dl class="dl-horizontal">
      <% if @venda.partner.id == Partner.ende.id %>
        <% infos = Ende.informacoes_parse(@venda.response_get, EndeUniqNumber.find(@venda.request_id)) %>
        <% infos.each do |info|%>
          <%= render "/ende/venda_recibo", info: info, title: "Venda de Crédito", venda: @venda %>
        <% end %>
      <% end %>
    </dl>

    <div class="page-header">
      <%= link_to 'javascript:history.go(-1)', class: 'btn btn-danger' do %>
        <span class="glyphicon glyphicon-list-alt"></span>
        Voltar
      <% end %>
    </div>
  </div>
</div>