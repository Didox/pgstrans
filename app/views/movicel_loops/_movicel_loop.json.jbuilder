json.extract! movicel_loop, :id, :usuario, :token, :uri, :ambiente, :agente, :terminal, :valor, :repeticao, :nropedidoinicio, :nropedido, :request, :response, :created_at, :updated_at
json.url movicel_loop_url(movicel_loop, format: :json)
