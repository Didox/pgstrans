json.extract! sub_agente, :id, :razao_social, :nome_fantasia, :bi, :industry_id, :morada, :bairro, :provincia, :email, :telefone, :created_at, :updated_at
json.url sub_agente_url(sub_agente, format: :json)
